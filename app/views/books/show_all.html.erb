<h1>All Books</h1>



<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Authors</th>
      <th>Average Rating</th>
      <th>Publisher</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= book.title %></td>
        <td><%= book.authors %></td>
        <td><%= book.average_rating %></td>
        <td><%= book.publisher %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<% if flash[:notice] %>
  <div class="alert alert-warning">
    <%= flash[:notice] %>
  </div>
<% end %>

<%= will_paginate @books %>
